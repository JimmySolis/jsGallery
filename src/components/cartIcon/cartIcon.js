import React from 'react';
import { Link } from 'react-router-dom'; // Import Link from react-router-dom
import { useCart } from '../cartContext/cartContext'; // Use the useCart hook
import './cartIcon.css';

const CartIcon = () => {
  // const { cartItems } = useCart(); // Access cart items from the context

  // return (
  //   // <div className="cart-icon">
  //   //   {/* Wrap the cart icon with a Link component to make it clickable */}
  //   //   <Link to="/cart">
  //   //     <span role="img" aria-label="cart">🛒</span>
  //   //     <span>{cartItems.length}</span> {/* Display the number of items in the cart */}
  //   //   </Link>
  //   // </div>
  // );
};

export default CartIcon;

///you momo 1) un highlight here so you can have the shoping cart icon back.